[variables]
CI = 'false'
NODE_OPTIONS = '--max-old-space-size=16384'
GENERATE_SOURCEMAP = 'false'

[phases.setup]
nixPkgs = ["nodejs_20", "curl", "wget", "nano"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=16384' npm run build"]

[start]
cmd = ''